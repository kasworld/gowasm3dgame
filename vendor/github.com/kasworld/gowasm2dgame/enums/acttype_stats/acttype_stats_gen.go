// Code generated by "genenum -typename=ActType -packagename=acttype -basedir=enums -statstype=int"

package acttype_stats

import (
	"bytes"
	"fmt"
	"html/template"
	"net/http"

	"github.com/kasworld/gowasm2dgame/enums/acttype"
)

type ActTypeStat [acttype.ActType_Count]int

func (es *ActTypeStat) String() string {
	var buf bytes.Buffer
	fmt.Fprintf(&buf, "ActTypeStats[")
	for i, v := range es {
		fmt.Fprintf(&buf,
			"%v:%v ",
			acttype.ActType(i), v)
	}
	buf.WriteString("]")
	return buf.String()
}
func (es *ActTypeStat) Dec(e acttype.ActType) {
	es[e] -= 1
}
func (es *ActTypeStat) Inc(e acttype.ActType) {
	es[e] += 1
}
func (es *ActTypeStat) Add(e acttype.ActType, v int) {
	es[e] += v
}
func (es *ActTypeStat) SetIfGt(e acttype.ActType, v int) {
	if es[e] < v {
		es[e] = v
	}
}
func (es *ActTypeStat) Get(e acttype.ActType) int {
	return es[e]
}

func (es *ActTypeStat) ToWeb(w http.ResponseWriter, r *http.Request) error {
	tplIndex, err := template.New("index").Funcs(IndexFn).Parse(`
		<html>
		<head>
		<title>ActType statistics</title>
		</head>
		<body>
		<table border=1 style="border-collapse:collapse;">` +
		HTML_tableheader +
		`{{range $i, $v := .}}` +
		HTML_row +
		`{{end}}` +
		HTML_tableheader +
		`</table>
	
		<br/>
		</body>
		</html>
		`)
	if err != nil {
		return err
	}
	if err := tplIndex.Execute(w, es); err != nil {
		return err
	}
	return nil
}

func Index(i int) string {
	return acttype.ActType(i).String()
}

var IndexFn = template.FuncMap{
	"ActTypeIndex": Index,
}

const (
	HTML_tableheader = `<tr>
		<th>Name</th>
		<th>Value</th>
		</tr>`
	HTML_row = `<tr>
		<td>{{ActTypeIndex $i}}</td>
		<td>{{$v}}</td>
		</tr>
		`
)
